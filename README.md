# My Notion

Personal Notion-style workspace. See `_bmad-output/` in the `my-manager` BMad hub for planning artifacts (PRD, Architecture Spine, SPEC).

- `my-notion-backend/` — NestJS API
- `my-notion-frontend/` — Next.js app
