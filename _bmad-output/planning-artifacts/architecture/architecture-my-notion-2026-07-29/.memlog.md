---
scope: My Notion - toàn bộ hệ thống (backend + frontend), dựa trên PRD final
purpose: build-substrate (giả định ban đầu — solo dev, cần xác nhận)
altitude: feature (giữ lại epics cho bước sau)
updated: 2026-07-30T09:02
---

- (event) Input: PRD final tại _bmad-output/planning-artifacts/prds/prd-my-notion-2026-07-29/ (prd.md + addendum.md) và Product Brief addendum tại _bmad-output/planning-artifacts/briefs/brief-my-notion-2026-07-29/addendum.md (đề xuất tech stack gốc của user + cấu trúc thư mục multi-project)
- (version) Đã verify qua web (2026-07-29): Next.js 16.2.x (stable, Active LTS tới 2027-10-22, React 19.2, Turbopack mặc định); NestJS v11 (Express v5 adapter mặc định); PostgreSQL 18.4 stable (19 beta dự kiến 09/2026); Node.js 24 = Active LTS hiện tại, Node 26 (phát hành 05/2026) vào LTS 10/2026
- (note) Đã verify qua web: BlockNote xây trên nền Tiptap (abstraction chồng abstraction) — dễ setup, có sẵn UX giống Notion + collab Yjs tích hợp sẵn, nhưng khó tuỳ biến sâu hơn. Tiptap (nền ProseMirror, 100+ extension) — full control, phải tự xây UI nhiều hơn, phù hợp khi cần tuỳ biến sâu
- (decision by user) A. Backend: Node.js + NestJS v11 (đồng ý với lean)
- (decision by user) B. Frontend: Next.js 16.2 (KHÔNG theo lean SPA/Vite của mình — user chọn Next.js)
- (decision by user) C. Editor: Tiptap (đồng ý với lean, ưu tiên tuỳ biến sâu hơn tốc độ)
- (decision by user) D. Auth: tự làm JWT + Google OAuth, self-hosted, không dùng Clerk/Auth0 (đồng ý với lean)
- (direction) Paradigm: Layered architecture (Controller-Service-Repository), map theo my-notion-backend/my-notion-frontend
- (decision by user) ORM: Prisma, dùng raw SQL ($queryRaw) cho recursive CTE cây trang
- (decision by user) Lưu nội dung Trang: 1 cột jsonb/Trang chứa toàn bộ Tiptap document (không tách bảng Block riêng) - [ADOPTED từ đề xuất gốc trong brief addendum]
- (decision by user) Repo: một git repo 'my-notion' chứa my-notion-backend/ và my-notion-frontend/, không dùng Nx/Turborepo
- (decision by user) CI/CD: GitHub Actions
- (decision by user) Invariant: Frontend không bao giờ truy cập DB trực tiếp - mọi mutation đi qua API/WebSocket của backend
- (event) Tất cả quyết định đã chốt (user: 'chốt'). Chuyển sang Finalize: distill spine từ memlog
- (version) Đã verify qua web (2026-07-29): Tiptap @tiptap/core 3.29.1 / @tiptap/react 3.29.2; Prisma ORM v7.4.2 (bản ổn định khuyến nghị production); Auth.js v5 (NextAuth) ổn định từ cuối 2024, production-ready 2026 — LƯU Ý: có tin Auth.js đang sáp nhập vào 'Better Auth', cần theo dõi khi implement, chưa ảnh hưởng tới việc dùng v5 hiện tại
- (decision) AD-7 (Database View = thuộc tính Page, không entity riêng) và Redis-not-needed-at-this-scale là quyết định mới phát sinh khi distill spine, chưa hỏi user trực tiếp - cần xác nhận ở bước triage
- (event) Draft ARCHITECTURE-SPINE.md v1 viết xong (7 AD, stack, ERD, source tree, capability map, deferred). Reconcile input: phát hiện thiếu React Query/Zustand trong Stack (từ brief addendum) - đã bổ sung
- (event) Reviewer Gate chạy 3 subagent song song: rubric walker, version-verify, adversarial (reviews/review-*.md). Tổng ~15 finding (1 critical, nhiều high). Đã tự áp fix rõ ràng: AD-4 tách rõ payload=tín hiệu (không kèm content), bổ sung full event set (created/updated/deleted/moved/bulk_changed), loại trừ self-echo, quy tắc refetch khi reconnect; AD-2 đóng băng node vocabulary v1 + src ảnh là absolute URL; AD-3 chốt ON DELETE CASCADE ở DB, CTE chỉ dùng để đọc/đếm; AD-5 viết lại đầy đủ: JWT ký HS256 qua AUTH_SECRET (không dùng JWE mặc định của Auth.js), đường truyền REST qua Next.js Route Handler + WS kết nối thẳng browser->backend, phiên 30 ngày sliding, disconnect khi hết hạn; ERD USERS thêm failed_login_attempts/locked_until; Deferred bổ sung view_schema chưa thiết kế đầy đủ + ràng buộc backup phải phủ cả ảnh
- (question) Chưa fix, để lại companion-doc riêng trước khi bắt đầu FR-11/12/13: hình dạng view_schema (column defs theo từng view_type, nơi lưu cell values, thứ tự kanban card), và quy tắc chuyển view_type trên trang đã có nội dung
- (event) lint_spine.py sạch (0 finding) sau khi fix
- (event) spine finalized
