---
topic: My Notion - SPEC kernel, distill từ PRD final + Architecture Spine final
updated: 2026-07-30T13:08
---

- (note) Input: PRD final (prd.md+addendum.md), Architecture Spine final (ARCHITECTURE-SPINE.md), Product Brief (brief.md+addendum.md) - tất cả đã có cấu trúc rõ (BMad-authored), distill trực tiếp không cần hỏi lại user
- (decision) companions (adopted): prd.md (giữ UJ, Glossary, testable consequences chi tiết hơn kernel) và ARCHITECTURE-SPINE.md (invariant kỹ thuật ràng buộc, spec KHÔNG lặp lại nội dung)
- (decision) sources (đã hấp thụ hết, không cần đọc lại): brief.md, brief/addendum.md, prd/addendum.md - nội dung cốt lõi đã chuyển vào Why/kernel (brief) hoặc đã bị Architecture Spine thay thế (2 addendum kỹ thuật)
- (capability) CAP-1 (FR-1) Đăng ký/đăng nhập - intent: đăng ký/đăng nhập bằng email+mật khẩu hoặc Google OAuth - success: đăng nhập tạo phiên hợp lệ; sai mật khẩu 5 lần liên tiếp khóa tạm 15 phút
- (capability) CAP-2 (FR-2) CRUD Trang - intent: tạo/đổi tên/xóa Trang - success: xóa Trang cha cascade xóa Trang con sau khi xác nhận qua hộp thoại, không có undo
- (capability) CAP-3 (FR-3) Cây trang & sidebar - intent: xem/điều hướng cây Trang lồng nhau qua sidebar - success: sidebar phản ánh đúng cấu trúc cha-con sau mọi thay đổi (tạo/xóa/di chuyển)
- (capability) CAP-4 (FR-4) Block editor cơ bản - intent: soạn thảo đoạn văn/tiêu đề/bullet/todo trong Trang - success: mỗi loại block giữ đúng định dạng qua reload; todo checkbox lưu trạng thái
- (capability) CAP-5 (FR-5) Auto-save - intent: tự lưu nội dung khi ngừng gõ, không cần thao tác Save - success: không mất nội dung đã gõ nếu đóng app ngay sau đó (trong khoảng debounce)
- (capability) CAP-6 (FR-6, Phase 2) Kéo-thả sắp xếp block - intent: đổi vị trí block bằng kéo-thả - success: thứ tự mới được lưu và giữ nguyên qua reload
- (capability) CAP-7 (FR-7, Phase 2) Slash command - intent: gõ / để mở menu chọn nhanh loại block - success: chọn loại block chèn đúng vị trí con trỏ
- (capability) CAP-8 (FR-8, Phase 2) Chuyển đổi loại block - intent: đổi block từ loại này sang loại khác - success: nội dung text giữ nguyên sau khi chuyển
- (capability) CAP-9 (FR-9) Upload ảnh cơ bản - intent: chèn ảnh vào block, lưu trên storage - success: ảnh hiển thị đúng sau reload và từ thiết bị khác (absolute URL, xem spine AD-2)
- (capability) CAP-10 (FR-10) Đồng bộ tức thời đa thiết bị - intent: thay đổi ở một thiết bị phản ánh gần-tức-thời ở thiết bị khác của cùng user qua WebSocket push (không phải multi-user collab) - success: sửa ở máy A, máy B thấy thay đổi mà không cần tải lại thủ công; thiết bị offline rồi online lại tự đồng bộ đúng trạng thái mới nhất, không nhân đôi/xung đột im lặng (xem spine AD-4)
- (capability) CAP-11 (FR-11, Phase 2) Database view Table - intent: hiển thị Trang con dạng bảng với cột tùy chỉnh - success: cột hiển thị đúng theo view_schema của Trang (schema chi tiết: xem spine Deferred)
- (capability) CAP-12 (FR-12, Phase 2) Database view Kanban - intent: hiển thị cùng dữ liệu dạng thẻ kéo-thả theo cột trạng thái - success: kéo thẻ sang cột khác cập nhật đúng trạng thái, giữ thứ tự sau reload
- (capability) CAP-13 (FR-13, Phase 3) Database view Calendar - intent: hiển thị dữ liệu theo lịch dựa trên cột ngày tháng - success: sự kiện hiển thị đúng ngày tương ứng
- (capability) CAP-14 (FR-14) Backup tự động - intent: sao lưu định kỳ toàn bộ dữ liệu Workspace, khôi phục được khi mất dữ liệu gốc - success: backup chạy daily, giữ 7 bản gần nhất, khôi phục thành công từ 1 bản backup bất kỳ trong 7 bản
- (capability) CAP-15 (FR-15, Phase 2) Markdown import/export - intent: xuất Trang ra file .md, nhập file .md có sẵn thành Trang mới - success: nội dung export/import khớp đúng node vocabulary v1 (xem spine AD-2), không mất block todo/ảnh
- (capability) CAP-16 (FR-16, Phase 3) Tìm kiếm toàn văn - intent: tìm nội dung trong tất cả Trang của Workspace - success: trả đúng kết quả chứa từ khóa tìm kiếm
- (capability) CAP-17 (FR-17, Phase 3) Comment trên Block - intent: gắn bình luận vào một Block cụ thể - success: bình luận hiển thị đúng gắn với Block, không sửa nội dung gốc
- (capability) CAP-18 (FR-18, Phase 3) Mention liên kết Trang - intent: gõ @ để chèn liên kết nhanh tới Trang khác - success: click mention điều hướng đúng tới Trang được liên kết
- (capability) CAP-19 (FR-19, Phase 3) Template có sẵn - intent: thư viện mẫu Trang dựng sẵn để nhân bản nhanh - success: tạo Trang mới từ template giữ đúng cấu trúc mẫu
- (capability) CAP-20 (FR-20, Phase 3) API công khai - intent: cho phép chương trình tự viết đọc/ghi dữ liệu qua API key - success: request kèm API key hợp lệ đọc/ghi đúng dữ liệu, request không có key bị từ chối
- (constraint) Không dùng vendor SaaS trả phí cho năng lực lõi (auth, hosting) - tự host, tự kiểm soát, tránh subscription là lý do chính dự án tồn tại
- (constraint) Phải chạy trên hạ tầng Linux server hiện có (Docker, PostgreSQL sẵn có, CI/CD qua GitHub Actions) - không dựng hạ tầng mới, không dùng dịch vụ cloud trả phí thêm
- (constraint) Một người phát triển duy nhất (thang-hub vừa là PM vừa dev) - độ phức tạp quy trình/tooling phải phù hợp quy mô solo, không kéo theo overhead nhóm
- (note) Non-goals lấy từ PRD §5: không multi-tenant/nhiều người dùng, không chia sẻ Trang cho người khác, không realtime multi-user collaboration (phân biệt CAP-10 chỉ đồng bộ cùng 1 user), không mobile app native, desktop app chưa nằm trong roadmap hiện tại, không trang giới thiệu/marketing công khai
- (note) Success signal lấy từ PRD §7: (1) thang-hub thực sự dùng My Notion hàng ngày thay vì mở lại tool cũ sau khi MVP chạy, (2) không có sự cố mất dữ liệu nhờ backup tự động; counter-metric: không lấy số tính năng đã build làm thước đo tiến độ
- (event) Self-Validate Pass 1 (Coherence): PASS - 20/20 capability có đủ intent+success; intents ở mức WHAT (chi tiết HOW đẩy qua companion spine); 3 constraint đều bend design thật; 6 non-goals; success signal testable (world-change + counter-metric); CAP ID liên tục không trùng/thiếu; prose lean
- (event) Self-Validate Pass 2 (Preservation): PASS - toàn bộ FR-1..FR-20 của PRD map đủ vào CAP-1..CAP-20; Non-Goals và Success Metrics của PRD map đủ; động lực cốt lõi của brief vào Why; đề xuất tech stack gốc + cấu trúc thư mục multi-project của brief addendum đã được Architecture Spine hấp thụ và thay thế (không mất nội dung, chỉ đổi nơi ở). Wrapper-only content không mang theo: văn phong narrative/cảm xúc của Executive Summary (brief) và rationale đã lỗi thời trong prd/addendum.md (đồng bộ đơn giản hoá ra MVP) - đã bị quyết định mới (spine AD-4) thay thế hoàn toàn
- (decision) Story 1: Auth (CAP-1). spec_checkpoint=true, done_checkpoint=false
- (decision) Story 2: Page CRUD + cây + sidebar (CAP-2, CAP-3). spec_checkpoint=true, done_checkpoint=false. invoke_dev_with: tuân theo spine AD-3 - cascade xóa ở cấp DB (onDelete Cascade), CTE chỉ để đọc/đếm
- (decision) Story 3: Block editor + auto-save (CAP-4, CAP-5). spec_checkpoint=true, done_checkpoint=false. invoke_dev_with: đóng băng node vocabulary v1 theo spine AD-2 (paragraph, heading 1-3, bulletList/listItem, taskItem chuẩn Tiptap, image) - không tự tạo custom node
- (decision) Story 4: Upload ảnh (CAP-9). spec_checkpoint=false, done_checkpoint=false. invoke_dev_with: src ảnh luôn absolute URL do backend phục vụ, theo spine AD-2
- (decision) Story 5: Đồng bộ đa thiết bị realtime (CAP-10). spec_checkpoint=true, done_checkpoint=true. invoke_dev_with: theo đúng spine AD-4 (payload WS chỉ là tín hiệu, đủ bộ event created/updated/deleted/moved/bulk_changed, loại trừ self-echo, bắt buộc refetch khi reconnect) và AD-5 (JWT HS256 qua AUTH_SECRET, verify ở handshake, disconnect khi hết hạn)
- (decision) Story 6: Backup tự động (CAP-14). spec_checkpoint=false, done_checkpoint=true (điểm kết thúc MVP). invoke_dev_with: nếu CAP-9 chọn lưu ảnh local disk, backup phải phủ luôn thư mục ảnh, không chỉ pg_dump
- (decision) Story 7: Block editor nâng cao - kéo-thả, slash command, đổi loại block (CAP-6, CAP-7, CAP-8). spec_checkpoint=false, done_checkpoint=false
- (decision) Story 8: Database view Table (CAP-11). spec_checkpoint=true, done_checkpoint=false. invoke_dev_with: view_schema chưa có schema chính thức - làm rõ hình dạng column definition + nơi lưu cell values trước khi viết story spec chi tiết (xem SPEC.md Open Questions + spine Deferred)
- (decision) Story 9: Database view Kanban (CAP-12). spec_checkpoint=true, done_checkpoint=false. invoke_dev_with: cùng vấn đề view_schema như story 8, cộng thêm cần trường thứ tự thẻ trong cột - không dùng updated_at/created_at để sort
- (decision) Story 10: Markdown import/export (CAP-15). spec_checkpoint=false, done_checkpoint=true (điểm kết thúc Phase 2)
- (decision) Story 11: Tìm kiếm toàn văn (CAP-16). spec_checkpoint=false, done_checkpoint=false. invoke_dev_with: công cụ tìm kiếm (Postgres FTS vs Meilisearch) chưa chốt - quyết định đầu story này
- (decision) Story 12: Database view Calendar (CAP-13). spec_checkpoint=false, done_checkpoint=false
- (decision) Story 13: Comment trên block (CAP-17). spec_checkpoint=false, done_checkpoint=false
- (decision) Story 14: Mention liên kết Trang (CAP-18). spec_checkpoint=false, done_checkpoint=false
- (decision) Story 15: Template có sẵn (CAP-19). spec_checkpoint=false, done_checkpoint=false
- (decision) Story 16: API công khai (CAP-20). spec_checkpoint=true, done_checkpoint=true (điểm kết thúc dự án). invoke_dev_with: expose dữ liệu ra ngoài qua API key - review kỹ trước khi code
- (event) stories.yaml rendered: 16 story, id 1-16 (quoted string, không trùng/không prefix-collide), không có field status, đủ title+description. Kiểm tra schema rules: PASS
